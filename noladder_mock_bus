#!/bin/bash
# Wrapper script for noladder_mock_bus.py
# Uses system Python with system-wide pymodbus

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/tools/noladder_mock_bus.py" "$@"
